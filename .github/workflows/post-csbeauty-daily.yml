name: Post GBP Daily - CS Beauty

on:
  schedule:
    - cron: '0 13 * * *'   # 9:00 AM EDT
  workflow_dispatch:

jobs:
  post-gbp:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install google-auth google-auth-oauthlib requests
      - name: Post to GBP - CS Beauty
        env:
          GBP_REFRESH_TOKEN: ${{ secrets.GBP_REFRESH_TOKEN_CSBEAUTY }}
          GBP_CLIENT_ID:     ${{ secrets.GBP_CLIENT_ID }}
          GBP_CLIENT_SECRET: ${{ secrets.GBP_CLIENT_SECRET }}
          GBP_CLIENTE:       csbeauty
        run: python post_gbp_action.py
